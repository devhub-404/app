---
import { VerifyEmailPage } from '@/features/auth/public';
import AppLayout from '@/app/ui/AppLayout.astro';
import { translate } from '@/features/auth/i18n';

const locale = Astro.locals.locale;
---

<AppLayout title={`DevHub - ${translate(locale, 'page.verifyTitle')}`} description={translate(locale, 'page.verifySubtitle')} keywords="devhub,auth">
  <VerifyEmailPage />
</AppLayout>
