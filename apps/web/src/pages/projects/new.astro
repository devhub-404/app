---
import { NewProjectPage } from '@/features/project/public';
import AppLayout from '@/app/ui/AppLayout.astro';
import { translate } from '@/features/project/i18n';

const locale = Astro.locals.locale;
---

<AppLayout title={translate(locale, 'newproject.devhubNewProject')} description={translate(locale, 'newproject.presentProject')} keywords="devhub,project">
  <NewProjectPage />
</AppLayout>
