---
import { AccountPage } from '@/features/account/public';
import AppLayout from '@/app/ui/AppLayout.astro';
import { translate } from '@/features/account/i18n';

const locale = Astro.locals.locale;
---

<AppLayout title={translate(locale, 'accountindex.devHubAccount')} description={translate(locale, 'accountindex.manageAccountDevHub')} keywords="devhub,account,settings">
  <AccountPage />
</AppLayout>
