---
import { AccountJobSuggestionsPage } from '@/features/account/public';
import AppLayout from '@/app/ui/AppLayout.astro';
import { translate } from '@/features/account/i18n';

const locale = Astro.locals.locale;
---

<AppLayout title={translate(locale, 'myjobsuggestions.pageTitle')} description={translate(locale, 'myjobsuggestions.trackSuggestions')} keywords="devhub,jobs,suggestions">
  <AccountJobSuggestionsPage />
</AppLayout>
