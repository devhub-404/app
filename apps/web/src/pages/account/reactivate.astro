---
import { AccountReactivationPage } from '@/features/account/public';
import AppLayout from '@/app/ui/AppLayout.astro';
import { translate } from '@/features/account/i18n';

const locale = Astro.locals.locale;
---

<AppLayout title={`DevHub - ${translate(locale, 'lifecycle.reactivationMetaTitle')}`} description={translate(locale, 'lifecycle.reactivationMetaDescription')} keywords="devhub,account">
  <AccountReactivationPage />
</AppLayout>
