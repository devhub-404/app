---
import { ResetPasswordPage } from '@/features/auth/public';
import AppLayout from '@/app/ui/AppLayout.astro';
import { translate } from '@/features/auth/i18n';

const locale = Astro.locals.locale;
---

<AppLayout title={`DevHub - ${translate(locale, 'page.resetTitle')}`} description={translate(locale, 'page.resetSubtitle')} keywords="devhub,auth">
  <ResetPasswordPage />
</AppLayout>
